import type { NextConfig } from 'next';

// Tüm sayfalar zaten `export const dynamic = 'force-dynamic'` ile çalışıyor;
// finans verisi hiçbir zaman önbelleğe alınmaz.
const nextConfig: NextConfig = {};

export default nextConfig;
